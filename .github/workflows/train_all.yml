name: Train All

on:
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  start:
    runs-on: ubuntu-latest
    steps:
      - name: Start training chain
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh workflow run train_bundle.yml -f bundle_id=0
